# Event Store Adapter Configuration
# Options: memory, kurrentdb
EVENT_STORE_ADAPTER=memory

# KurrentDB Connection String (only used when EVENT_STORE_ADAPTER=kurrentdb)
KURRENTDB_CONNECTION_STRING=kurrentdb://localhost:2113?tls=false
